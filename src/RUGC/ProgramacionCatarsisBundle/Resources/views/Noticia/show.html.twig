{% extends '::base.html.twig' %}

{% block body -%}

    <div class="divNoticia">
        <label>{{ entity.autor }} | {% if entity.fecha %}{{ entity.fecha|date('d-m-Y H:i:s') }}{% endif %}</label>
        <h1>{{ entity.titulo }}</h1>
        {{ entity.contenido | raw }}                

    </div>
    {#    <table class="record_properties">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ entity.id }}</td>
                </tr>
                <tr>
                    <th>Titulo</th>
                    <td>{{ entity.titulo }}</td>
                </tr>
                <tr>
                    <th>Fecha</th>
                    <td>{{ entity.fecha|date('Y-m-d H:i:s') }}</td>
                </tr>
                <tr>
                    <th>Contenido</th>
                    <td>{{ entity.contenido }}</td>
                </tr>
                <tr>
                    <th>Autor</th>
                    <td>{{ entity.autor }}</td>
                </tr>
                <tr>
                    <th>Estado</th>
                    <td>{{ entity.estado }}</td>
                </tr>
            </tbody>
        </table>#}
    <a  href="{{ path('noticia') }}"><input class="enlaceNoticias" type="button" value="Ver todas las noticias"/></a>

    {%if is_granted('ROLE_ADMIN')%}
        <a href="{{ path('noticia_edit', { 'id': entity.id }) }}"><input class="btnDer" type="button" value="Editar"/></a>

        {{ form(delete_form) }}
    {% endif %}

{% endblock %}
