{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Generaciones</h1>
    {{ entity.texto | raw}}
    {#<table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Texto</th>
                <td>{{ entity.texto }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('generaciones') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('generaciones_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>#}
{% endblock %}
