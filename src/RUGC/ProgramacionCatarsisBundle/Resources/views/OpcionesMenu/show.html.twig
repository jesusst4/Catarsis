{% extends '::base.html.twig' %}

{% block body -%}
    <h1>OpcionesMenu</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombreopcion</th>
                <td>{{ entity.nombreOpcion }}</td>
            </tr>
            <tr>
                <th>Ruta</th>
                <td>{{ entity.ruta }}</td>
            </tr>
            <tr>
                <th>Prioridad</th>
                <td>{{ entity.prioridad }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('opcionesmenu') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('opcionesmenu_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
