{% extends '::base.html.twig' %}

{% block body -%}
    {{ form(edit_form) }}

    {#<ul class="record_actions">
        <li>
            <a href="{{ path('definiciones') }}">
                Back to the list
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>#}
    {{ tinymce_init() }}
{% endblock %}
