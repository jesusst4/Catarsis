<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Grupo Catarsis{% endblock %}</title>
        {% block stylesheets %}
            {% stylesheets '@RUGCProgramacionCatarsisBundle/Resources/public/css/*' filter='cssrewrite' %}
            <link rel="stylesheet" href="{{ asset_url }}" />
            {% endstylesheets %}

            {%javascripts '@RUGCProgramacionCatarsisBundle/Resources/public/JS/*' %}
            <script type="text/javascript" src="{{ asset_url }}"></script>
            {% endjavascripts %}
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />

    <div class="divBanner">        
    </div>

    <div id="menubar">
        <ul id="menu-horizontal">
            <li><a href="{{path('contenido_show', {'id':1})}}" title="Texto">CONOZCANOS</a></li>
            <li><a href="" title="Texto">PROGRAMACIÓN:RADIO/TV</a>
                <ul>  
                    <li><a href="{{path('programacion')}}" title="Texto">MES ACTUAL</a></li>
                    <li><a href=""> BUSCAR</a></li>
                        {% if is_granted('ROLE_ADMIN') %}
                        <li><a href ="{{path('programacion_selectMonth')}}" title = "Texto">EDITAR</a></li>
                        {% endif %}
                </ul>
            </li>
            <li><a href="{{path('contenido_show', {'id':2})}}" title="Texto">DEFINICIONES</a></li>
            <li><a href="{{path('contenido_show', {'id':4})}}" title="Texto">GRUPOS NACIONALES</a></li>
            <li><a href="{{path('contenido_show', {'id':3})}}" title="Texto">GENERACIONES CATÁRTICAS</a></li>
            <li><a href="{{path('contenido_show', {'id':1})}}" title="Texto">NOTICIAS</a></li>
            <li><a href="{{path('contenido_show', {'id':1})}}" title="Texto">ENLACES RECOMENDADOS</a></li>
        </ul>
    </div>
</head>

<body>
    <div class="contenedor">
        {% block body %}           
        {% endblock %}

        {% block footer %}
            <div class="footer">                
            </div>
        {% endblock %}
    </div>
</body>

</html>
